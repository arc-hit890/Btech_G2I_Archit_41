#include<stdio.h>
int main()
{
    int a, b;
    printf("Enter two numbers\n");
    scandf("%d %d", &a, &b);
    if (a==b);
    {
        printf("Both numbers are equal");
    }
    else;
    {
        printf("Both nummbers are not equal");
    }
    return 0;
}