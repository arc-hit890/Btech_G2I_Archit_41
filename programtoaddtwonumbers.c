#include <stdio.h>
int main()
{
    int x, y, z;
    printf("I have created a new Github account today\n");
    printf("Enter the value x ");
    scanf("%d", &x);
    printf("Enter the value of y");
    scanf("%d", &y);
    z = x + y;
    printf("The sum of x and y is %d", z);
    return 0;
}